<h1>タスク一覧</h1>

<ul>
  <% @usertasks.each do |usertask| %>
    <li><%= link_to usertask.id, edit_usertask_path(usertask) %> : <%= usertask.content %> > <%= usertask.status %></li>
  <% end %>
</ul>


<%= paginate @usertasks %>
<%= link_to 'タスクの追加', new_usertask_path(), class: 'btn btn-lg btn-primary' %>